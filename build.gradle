plugins {
   id "com.github.ben-manes.versions" version "0.42.0"
}

subprojects {
   group = 'io.noumennal'

   repositories {
      mavenCentral()
      maven {
         name 'plugins'
         url "https://plugins.gradle.org/m2/"
      }
   }
}
