pluginManagement {
   repositories {
      maven {
         url "s3://nio-maven"
         authentication {
            awsIm(AwsImAuthentication)
         }
      }
      gradlePluginPortal()
   }
}
rootProject.name = 'internal-stage'
